import type {World} from '../shared/schemas/oil-crisis.ts';
import {agendaEngine} from '../server/src/agenda/agenda.ts';
const esc=(s:string)=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
export function agendaMarkup(world:World){const tasks=agendaEngine.tasks(world),done=tasks.filter(t=>t.done).length;return `<section class="daily-agenda"><span class="case-tag">TODAY’S AGENDA · ${done} OF ${tasks.length} COMPLETE</span><h2>One day.<br>A few important calls.</h2><p class="speech">Finish these decisions, then end the day. Meetings and decisions stay in today; fuel consumption, revenue and new headlines are processed together overnight.</p><div class="agenda-tasks">${tasks.map(t=>`<button class="agenda-task ${t.done?'done':''}" data-activity="${esc(t.panel)}"><span aria-hidden="true">${t.done?'✓':'○'}</span><span><strong>${esc(t.title)}</strong><small>${t.done?'Completed · Review your decision':esc(t.reason)}</small></span><b>↗</b></button>`).join('')}</div><button id="end-day" class="reply-button" ${done!==tasks.length||!tasks.length?'disabled':''}>${done===tasks.length?'End day →':'Complete the agenda to end day'}</button><p class="aside-bottom">Buying or funding a proposal spends resources immediately. Declining a deal is a valid decision. Opening a meeting alone does not complete it.</p><p id="agenda-error" role="alert"></p></section>`;}
