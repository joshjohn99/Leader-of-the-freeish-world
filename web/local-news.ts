import type {World} from '../shared/schemas/oil-crisis.ts';
import {escapeHtml as e} from './conversation.ts';
export function localNewsMarkup(world:World){const bulletins=world.events.filter(x=>x.localNews).map(x=>x.localNews!).reverse();return `<div class="feed-brand"><span class="bull-logo">BULL</span><div><h2>The neighborhood is talking.</h2><p>Local headlines. Loud opinions. Confirmed facts only.</p></div></div><p class="fineprint">BULL covers fictional communities across Freedoma. It avoids graphic detail and does not invent suspects, motives, or outcomes.</p>${bulletins.length?bulletins.map(news=>`<article class="news-card local-news-card"><span class="case-tag">LOCAL DESK · ${e(news.community)} · DAY ${news.day}</span><h3>${e(news.headline)}</h3><p>${e(news.facts)}</p><div class="note"><b>Why it matters locally</b><br>${e(news.stakes)}</div><small>No automatic policy change. Your cabinet may bring this back to the desk.</small></article>`).join(''):'<div class="note">No local bulletin yet. BULL starts reporting as the days advance.</div>'}<button class="text-button" id="leave-local-news">Back to my desk ↗</button>`;}
